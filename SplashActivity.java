import android.content.Intent;\nimport android.os.Bundle;\nimport android.os.Handler;\nimport android.view.View;\nimport android.view.animation.AlphaAnimation;\nimport android.widget.TextView;\nimport androidx.appcompat.app.AppCompatActivity;\n\npublic class SplashActivity extends AppCompatActivity {\n    @Override\n    protected void onCreate(Bundle savedInstanceState) {\n        super.onCreate(savedInstanceState);\n        setContentView(R.layout.activity_splash);\n\n        TextView splashText = findViewById(R.id.splash_text);\n        splashText.setText(\"You're Welcome\");\n\n        // Fading animation from left to right\n        splashText.setVisibility(View.INVISIBLE);\n        splashText.setTranslationX(-500);\n        splashText.setAlpha(0f);\n        splashText.setVisibility(View.VISIBLE);\n        splashText.animate()\n                .translationX(0)\n                .alpha(1f)\n                .setDuration(2000)\n                .start();\n\n        // Transition to MainActivity after 3 seconds\n        new Handler().postDelayed(new Runnable() {\n            @Override\n            public void run() {\n                Intent intent = new Intent(SplashActivity.this, MainActivity.class);\n                startActivity(intent);\n                finish();\n            }\n        }, 3000);\n    }\n}